# Switchboard Configuration
# Copy to .env and edit as needed

# Port to bind on (default: 19400)
PORT=19400

# Database path (used inside container, override if needed)
SWITCHBOARD_DB=/data/switchboard.db
